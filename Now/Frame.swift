import Foundation

class Frame {
/*   var
  @now = Date.now()
  @was = Date.now()
  
  measure: ->
  @now   = Date.now()
  @delay = @now - @was
  @was   = @now
  
  animate: (draw) ->
  @timer = window.requestAnimationFrame draw
  
  cancel: ->
  window.cancelAnimationFrame @timer
  

  
  init() {
    
  } */
}